[
  {
    "name": "app",
    "image": "${ecs_image_id}:${ecs_image_version}",
    "cpu": 10,
    "memory": 512,
    "essential": true,
    "portMappings": [
      {
        "containerPort": tonumber("${container_port}"),
        "hostPort": tonumber("${host_port})"
      }
    ]
  }
]
